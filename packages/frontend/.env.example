# ============================================================================
# VINCENT CONFIGURATION
# Must match backend configuration
# Get from https://dashboard.heyvincent.ai/
# ============================================================================

# Your Vincent App ID (required)
# Must be the same as backend VINCENT_APP_ID
VITE_VINCENT_APP_ID=your_app_id_here

# OAuth redirect URI (required)
# Development: http://localhost:5173/callback
# Production: https://app.volvi.xyz/callback
# Must be registered in Vincent Dashboard under "Redirect URIs"
VITE_REDIRECT_URI=https://app.volvi.xyz/callback

# ============================================================================
# BACKEND API CONFIGURATION
# ============================================================================

# Backend API URL (required)
# Development: http://localhost:3001
# Production: Your Railway backend URL (e.g., https://volvi-backend.up.railway.app)
VITE_BACKEND_URL=http://localhost:3001

# ============================================================================
# BLOCKCHAIN NETWORK CONFIGURATION
# ============================================================================

# Chain ID (required)
# Must match backend CHAIN_ID
# Base Sepolia testnet: 84532
# Base Mainnet: 8453
VITE_CHAIN_ID=8453

# RPC URL for blockchain access (required)
# Used for reading contract state from frontend
# Base Sepolia: https://sepolia.base.org
# Base Mainnet: https://mainnet.base.org
# Or use Alchemy: https://base-mainnet.g.alchemy.com/v2/YOUR_API_KEY
VITE_RPC_URL=https://mainnet.base.org

# Options Protocol contract address (required after deployment)
# Must match backend OPTIONS_PROTOCOL_ADDRESS
# Base Mainnet: 0x0c239d161780747763E13Bee4366Ad44D347608F
# Base Sepolia (testnet): 0xD7AFfB2B3303e9Cb44C9d9aFA6bD938200b3C8F2
VITE_OPTIONS_PROTOCOL_ADDRESS=0x0c239d161780747763E13Bee4366Ad44D347608F

# ============================================================================
# ENVIRONMENT
# ============================================================================

# Application environment (development/production)
# development: Show detailed errors, enable debug features
# production: Hide sensitive info, optimize performance
VITE_ENV=production

# ============================================================================
# SETUP INSTRUCTIONS
# ============================================================================

# 1. Copy this file to .env:
#    cp .env.example .env

# 2. Get Vincent App ID:
#    - Visit https://dashboard.heyvincent.ai/
#    - Create new app or use existing
#    - Copy App ID to VITE_VINCENT_APP_ID
#    - Add redirect URI (http://localhost:5173/callback) in app settings

# 3. Configure backend URL:
#    - Development: http://localhost:3001
#    - Production: Your deployed backend URL

# 4. Set contract address:
#    - Deploy smart contracts first (see backend setup)
#    - Copy deployed address to VITE_OPTIONS_PROTOCOL_ADDRESS
#    - Must match backend configuration

# 5. Start the development server:
#    pnpm dev
#    Opens at http://localhost:5173

# ⚠️  IMPORTANT NOTES:
# - All VITE_* variables are embedded in the build at build time
# - These variables are PUBLIC (visible in browser)
# - Never put secrets in frontend .env (no private keys!)
# - VITE_VINCENT_APP_ID must match backend VINCENT_APP_ID
# - VITE_OPTIONS_PROTOCOL_ADDRESS must match backend
# - VITE_REDIRECT_URI must be registered in Vincent Dashboard
